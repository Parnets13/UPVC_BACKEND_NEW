services:
  - type: web
    name: upvc-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    # Add your MongoDB connection string and other environment variables in Render dashboard



